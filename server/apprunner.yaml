version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building backend application with Docker"
run:
  runtime-version: latest
  command: npm start
  network:
    port: 5000
    env:
      - name: PORT
        value: "5000"
      - name: NODE_ENV
        value: "production"
  env:
    - name: MONGODB_URI
      value: "mongodb+srv://root:GftJKZ2rlsJlk9DR@cluster0.6rjyw.mongodb.net/sample_airbnb?authSource=admin&replicaSet=atlas-l540kb-shard-0&readPreference=primary&ssl=true"